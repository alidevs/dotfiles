-- Order here preserves the exact left-to-right layout of the original config

-- Left
require("items.spaces")
require("items.front_app")

-- Center
require("items.spotify")

-- Right (first added = far right)
require("items.calendar")
require("items.battery")
require("items.brew")
require("items.volume")
require("items.cpu")
